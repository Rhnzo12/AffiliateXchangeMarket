declare module '@paypal/payouts-sdk' {
  // Minimal ambient declaration to allow TypeScript compilation.
  // For full typing, install official @types if available or expand this file.
  export const core: any;
  export const payouts: any;
  const mod: any;
  export default mod;
}
